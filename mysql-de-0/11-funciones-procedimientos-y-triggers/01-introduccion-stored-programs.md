# 01 — Stored Programs

Procedures ejecutan acciones y se invocan con `CALL`. Functions devuelven un valor. Triggers se ejecutan automáticamente en INSERT, UPDATE o DELETE. En clientes como Workbench se usa `DELIMITER` para definir bloques que contienen `;`.
