# 05 — Bases y schemas

En MySQL `CREATE DATABASE` y `CREATE SCHEMA` son sinónimos. No se usa normalmente una jerarquía de schemas dentro de la misma base como en PostgreSQL. Para separar dominios, usa tablas con prefijos claros o bases diferentes.
