# Proyecto final tienda MySQL

Orden: 02 base, 03 tablas, 04 datos, 05 a 09 consultas, 10 function, 11 procedure, 12 trigger y 13 índices.

Ejemplo:

```sql
CALL sp_crear_pedido_simple(1,1,2);
SELECT fn_total_pedido(1);
```
